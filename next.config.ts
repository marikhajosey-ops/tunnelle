import type { NextConfig } from "next";

// NanaOne v1.0.3 - Limits & Curator Deployment
const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  // Deployment Marker (Sat Apr 4 00:24:00 2026): Force Curator Deployment
};

export default nextConfig;
